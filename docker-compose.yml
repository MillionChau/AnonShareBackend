services:
  web:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: anon-share-backend

    ports:
      - "5340:5340"

    env_file:
      - .env

    restart: unless-stopped
